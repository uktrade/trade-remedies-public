version: "2"
services:
  cli:
    build: .
    stdin_open: true
    tty: true
    env_file:
      - local.env
    ports:
      - "8002:8002"
    volumes:
      - .:/opt/traderemedies/public
    environment:
      DEBUG: 'True'
      VCAP_SERVICES: '{"redis":[{"credentials":{"uri":"redis://localhost:6379"}}]}'
      REDIS_DATABASE_NUMBER: 1
      API_BASE_URL: http://localhost:8000
    entrypoint:
      - /opt/traderemedies/public/start.sh
      - cli
    network_mode: host
